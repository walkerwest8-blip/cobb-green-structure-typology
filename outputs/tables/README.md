Summary of green structure classes by count and total area (acres). Residual patches dominate numerically, while corridors account for the majority of total green area.
Counts and total area of corridor features exceeding the core size threshold (>40 acres), identifying 12 large linear features that function as de facto cores.
